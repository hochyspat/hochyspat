#include <iostream>

using namespace std;

int main()
{
	int n;
	cin >> n;
	int minn = 1000000;
	int* arr = new int[n];
	for (int i = 0; i < n; ++i)
	{
		cin >> arr[i];
		if (arr[i] < minn)
			minn = arr[i];
	}
	cout << minn;
}
