#include <iostream>
#include <string>
using namespace std;

int main()
{
    int number;
    cin >> number;
    int summ = 0;
    while (number > 0) {
        summ += number % 10;
        number /= 10;
    }
    cout << summ;
}
