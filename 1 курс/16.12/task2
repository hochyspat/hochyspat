#include <iostream>

using namespace std;

int main()
{
	int array[5] = { 2, 2, 3, 4, 2 };
	int count = 0;
	for (int i = 0; i < 5; ++i)
	{
		if (*(array + i) != *(array + 4))
			count++;
	}
	cout << count;
}
