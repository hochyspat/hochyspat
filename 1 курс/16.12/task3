#include <iostream>

using namespace std;

int main()
{
	int array[5] = { 1, 2, 3, 4, 5 };
	int reversedArray[5];
	for (int i = 0; i < 5; ++i)
		*(reversedArray + i) = *(array + (4 - i));
	for (int i = 0; i < 5; ++i)
		cout << *(reversedArray + i) << " ";
}
