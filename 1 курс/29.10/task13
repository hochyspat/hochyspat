#include <iostream>
#include <set>

using namespace std;

int main()
{
    int array1[5]{};
    set<int> num{};

    for (int i = 0; i < size(array1); ++i)
    {
        cin >> array1[i];
        num.insert(array1[i]);
    }

    if (size(array1) != size(num))
        cout << "YES" << endl;
    else
        cout << "NO" << endl;
}
