#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    double array1[5]{};

    for (int i = 0; i < size(array1); ++i)
    {
        cin >> array1[i];
        if (i != 0 && array1[i] > array1[i - 1])
            cout << array1[i] << endl;
    }
}
