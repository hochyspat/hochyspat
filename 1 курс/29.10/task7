#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    double array1[5];
    int count = 0;
    for (int i = 0; i < size(array1); ++i)
    {
        cin >> array1[i];
        if (array1[i] > 0)
            count += 1;
    }

    cout << count << endl;
}
