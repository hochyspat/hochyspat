#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    double array1[5];
    double summ = 0;

    for (int i = 0; i < size(array1); ++i)
    {
        cin >> array1[i];
        summ += array1[i];
    }

    double average = summ / size(array1);

    for (int i = 0; i < size(array1); ++i)
    {
        if (array1[i] > average)
            cout << array1[i] << " ";
    }

}
