#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    double array1[5]{};
    int a, b;
    cin >> a >> b;

    for (int i = 0; i < size(array1); ++i)
    {
        cin >> array1[i];
        if (array1[i] > a && array1[i] < b)
            cout << i << " ";
    }
}
