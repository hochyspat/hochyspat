#include <iostream>
#include <cstdlib>

using namespace std;

int main() 
{
    int* array = new int[5];
    for (int i = 0; i < 5; ++i)
        array[i] = rand();
    for (int i = 0; i < 5; ++i)
        cout << array[i] << " ";
}
