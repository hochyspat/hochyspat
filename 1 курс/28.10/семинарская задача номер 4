#include <iostream>

using namespace std;

int main()
{
    double a, b, c, d;
    cin >> a >> b;
    cin >> c >> d;

    if ((max(a, c) - min(b, d)) <= 0)
        cout << max(a, c) << " " << min(b, d) << endl;
    else
        cout << "No intersection" << endl;
}
